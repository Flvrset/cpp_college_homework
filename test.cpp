#include <iostream>

using namespace std;

int main()
{
    int first_number, even_number, answer;
    cin >> first_number;
    while (cin >> answer) {
   if (answer == first_number)
    {
        break;
     }
     if (answer % 2 == 0) {
     }}
}